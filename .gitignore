CLAUDE.md
potential_bugs.md
plan.md
.claude/
build/
build-iouring/
# Artifacts left behind by the test suite.
tests-suite.db*
tests-sqlite-types.db*
pruebas-suite.db*
example/notes.db*
# Benchmark scratch data, not part of the project.
bench

# --native (COMPILACION-NATIVA.md, section 11): the .cpp/.so that
# compile_native() generates, regenerated on every --native startup.
.lux-native/
